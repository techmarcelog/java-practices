# Pointers (Referências em Java)

Em Java não existem ponteiros explícitos como em C/C++, mas variáveis de tipos de referência apontam para objetos na memória.

## Exemplo
Classe: `PointerConceptDemo`
